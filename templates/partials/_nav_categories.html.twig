

{% for category in categories %}
    <a class="nav-item nav-link link-body-emphasis active"
       href="{{ path('app_category_show', {'id': category.id}) }}">
        <button class="btn btn-{{ category.color }}">{{ category.name }}</button>
    </a>
{% endfor %}